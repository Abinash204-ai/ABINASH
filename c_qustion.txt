1) Write a simple C program to print a 5x5 pattern of stars using only one loop.
----------------------------------------------------------------------------------


2) What will be the output of the following code? 
-------------------------------------------------

#include <stdio.h>

int *add(int a, int b);

int main() {
    int a = 10, b = 5, *p;
    
    p = add(a, b); 
    
    printf("hello world x = %d\n", *p);  
    
    return 0;
}

int *add(int a, int b) {  
    int c;  
    
    c = a + b;  
    
    printf("hello world c = %d\n", c);  
    
    return &c;  
}


3)I want to print the global variable and expect 10 as the output?. You can add any other lines in the code, but do not modify the existing variable names or lines!.
-------------------------------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

int a = 10;  

int main() {

    int a = 5; 

    printf("Global value: %d\n", a);  

    return 0;
}


4)What will be the output of the following code?
-------------------------------------------------

#include <stdio.h>

void fun(int *a) {
    *a = *a * 5;
}

int main() {
    int a = 10;
    fun(&a);
    fun(&a);
    printf("Updated value: %d\n", a);
    return 0;
}

5)What will be the output of the following code? assume a address is 1000!,
---------------------------------------------------------------------------

#include <stdio.h>

int main() {
    int a = 5;
    int *p = &a;
    *p = *p + 2;
    printf("a value = %d a address = %p p address = %p \n",a ,&a ,p);
    return 0;
}


6)What will be the output of the following code? 
-------------------------------------------------

#include <stdio.h>

int main() {
    char str[] = "Hello World";
    int *ptr = (int *)str;
    ptr++;
    printf("%s\n", (char *)ptr);
    return 0;
}


7)What will be the output of the following code? 
------------------------------------------------

#include <stdio.h>

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int *ptr = arr + 1;
    
    ptr += 2;
    printf("%d\n", *ptr);
    
    return 0;
}


8)What will be the output of the following code? 
-------------------------------------------------

#include <stdio.h>

int main() {
    int a = 10;
    int *p;
    
    *p = &a;
    
    *p = *p + 500; 
    
    printf("Value: %d\n", *p);
    
    return 0;
}

9)What will be the output of the following code? assume a address is 1000!,b address is 2000!  
---------------------------------------------------------------------------------------------

#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    int *ptr1 = &a;
    int *ptr2 = &b;
    
    ptr1 = ptr2;
    ptr2 = &ptr1;
    
    printf("a value = %d b value = %d\n", *ptr1,*ptr2);
    
    return 0;
}

10)What will be the output of the following code? 
------------------------------------------------

#include <stdio.h>

int main() {
    int a = 5, b = 3;
    
    printf("a & b = %d\n", a & b);
    printf("a | b = %d\n", a | b);
    printf("a ^ b = %d\n", a ^ b);
    printf("~a = %d\n", ~a);
    
    return 0;
}
































